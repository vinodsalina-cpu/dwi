# Current progress

- Active phase: Phase 5 — POC complete; Phase 6 local POC qualification complete
- Phase owner: Codex
- Starting SHA: `ff0bbea30b2e502bc3c5b9287da003994fc13ce0`
- Independent Phase 6 review baseline: `c8d1723`; checkpoint `aa2a1ba` contains one-container/concurrent-consent hardening, and the final local tip adds the sole Prompt Optimizer identity, Step 1 control order, packaging evidence, and updated installed verifiers.
- State: Phase 5 and Phase 6 local POC matrices pass. Phase 4 semantic/fixed-executor qualification remains blocked and is not implied.

## Completed

- Consolidated optimizer recents under the workspace-scoped session authority and purged legacy global recents after one-time import (`3b45b60`).
- Added a production-fail-closed installed-smoke confirmation port and CDP-driven packaged journey (`afbd113`). VS Code 1.134.0 proves initialization, Project Meta Context review/confirmation, provider-free deterministic Resolve/Review, save-to-recents, and optimizer-only reset from the installed artifact under development-mode automatic confirmations; it does not exercise production modal consent UI.
- Final full suite after one-icon hardening: 420 tests; all six typechecks; schema export; build; 388-file VSIX SHA-256 `114fc30d850036d1ceb7cadea0845aa43b0e3f5e0dd1e416609cd071568ce245`; same-process installed functional flow; 16 proxy syntax checks, 20 proxy tests, and recovered simulator scenarios.
- Broad local Phase 6 failure/structural matrix passes malformed response, timeout, cancellation, stale revision/result, auth, quota/rate limit, refusal/truncation, connectivity, policy/safety, hash mismatch, locked section, migration/downgrade, privacy, call ceiling, provenance/conflict, rollback, and repository-independence coverage. Together with the packaged restart pass below, this closes the local Phase 6 POC matrix.
- Added the isolated cross-process installed verifier and local command (`529516e`). It installs the VSIX into a disposable VS Code 1.134.0 portable profile, drives the production native consent sheets, saves a deterministic optimizer review, restarts the same profile, verifies Project Meta Context and optimizer recovery, resets only optimizer state, and verifies reset persistence on a second restart.
- The exact fresh VSIX passed the three-process installed journey: `DWI_SMOKE_RESTART_INSTALL_OK`, `DWI_SMOKE_RESTART_PROJECT_READY`, `DWI_SMOKE_RESTART_SEED_OK`, `DWI_SMOKE_RESTART_CONTEXT_RESTORED`, `DWI_SMOKE_RESTART_SESSION_RESTORED`, `DWI_SMOKE_RESTART_RESET_OK`, `DWI_SMOKE_RESTART_RESET_PERSISTED`, and `DWI_SMOKE_RESTART_EXTENSION_OK`.
- The final identity correction contributes only the Prompt Optimizer native view under `dwi-sidebar`. Home and Project Meta Context remain internal destinations, the hidden legacy open command selects internal Home, and the sole view is rehydrated from host-owned state before internal route selection. The restart verifier removes its ephemeral profile in `finally`.
- The authorized main VS Code profile now contains exactly `dwi-poc.developer-work-intelligence@0.1.0` at `/Users/vk/.vscode/extensions/dwi-poc.developer-work-intelligence-0.1.0`; no duplicate DWI package or obsolete marker is present, and the running window was left open.

- Preserved Phase 4 as blocked and created a verified compact checkpoint containing a complete Git bundle at `ff0bbea30b2e502bc3c5b9287da003994fc13ce0`, local program/evaluation records, approval boundaries, and SHA-256 verification.
- Created local branch `feature/po-05-product-hardening` at the exact Phase 4 functional SHA and froze the Phase 5 contract without changing product code.
- Inventoried the live persistence seams and revalidated `localFingerprint` as the intentionally checkout-local optimizer-session key. The inventory distinguishes live stores from dormant V2 schema types and freezes optimizer-only reset boundaries.
- Added and wired the host-owned `dwi.promptOptimizer.sessions.v1` store with exact-key validation, 50-session/256-KiB-per-session bounds, five recents per session, optimistic revisions, checkout-local isolation, safe unknown/corrupt handling, and workspace-scoped reset. It is the preferred recovery authority for draft/candidate/review/view/recents; legacy state remains a downgrade-compatible mirror.
- Added one-time legacy migration, safe unknown-version refusal, current session hydration, dependency invalidation on project refresh/review/brief changes, and an optimizer-only reset protocol/UI that explicitly retains the approved project brief, declaration, consent, and provider settings.
- Independent review corrections make optimizer-only reset require the exact approved/current project review plus confirmed compilable brief in both host authority and snapshot mutation, while the webview exposes it only in corresponding ready states. The session cap now measures safe serialized UTF-8 bytes rather than JavaScript string code units.
- Corrected local suite passes 416 tests (104 prompt optimizer + 31 workspace + 66 core + 16 catalog + 168 host + 31 webview); all six package typechecks, unchanged schema, build, VSIX packaging, archive inspection, `git diff --check`, isolated activation/open smoke, and standard-install activation/open smoke pass.
- Corrected VSIX before standard-install verification: 387 files, 683,017 bytes, SHA-256 `7f9acd71af8b21fb2644a1b1382d9d0e6aa794aa9ce30e0c131367b84538349b`.
- DWI-PO-0029 prevents automatic brief-confirmed and workspace-root lifecycle messages from replacing an already-active Prompt Optimizer surface. Rewrite dispatch and pending also explicitly retain the optimizer; regression coverage exercises dispatch, both lifecycle interleavings, pending, actionable error, fallback, and result while preserving the task draft.
- Final route-corrected VSIX: 387 files, 683,053 bytes, SHA-256 `a2d93c51e30ca42666f5cf3374a69a7f1a51218b21bb0c02ea9f6f868b7dea0e`. The 416-test suite, six typechecks, unchanged schema, diff check, package archive, and isolated activation/Home/Prompt Optimizer smoke pass. Standard VS Code has `dwi-poc.developer-work-intelligence@0.1.0` installed with a byte-identical webview bundle.
- DWI-PO-0027 keeps deterministic attention/context allocation authoritative for the current product route and preserves learned models, tiny local models, Cactus/Needle, autoresearch, adaptive self-training, and related ideas as gated Phase 7 experiment candidates only. It does not change any current phase status or gate.

- Phases 1–3 are complete under their recorded local/exact evidence.
- Phase 4 contract and DWI-PO-0014 same-call projection amendment are frozen locally.
- DWI-PO-0021 freezes the exact ten-case A/B/C corpus, model-host state, no-retry budgets, lane-blind scoring, and objective enhancement thresholds.
- Six evaluator tests and the final zero-network dry run pass: ten A/B byte identities, eight production orchestration paths through a fake provider, two mandatory no-egress fallbacks, and 30 strict fixed-executor request constructions.
- One explicit LLM action now uses the host-only provider port and V2 request → strict response → hash-bound bounded patch → deterministic compiler path.
- Local fallback, cancellation, timeout, truncation, authentication/rate-limit/provider error, malformed schema/JSON, wrong hash, locked section, secret-like content, stale identity, and no-fourth-call behavior are covered.
- Same-call projection uses the exact snake_case wire contract and validates estimation ID/status, end-to-end totals, six-part breakdowns, deltas, optional supplied-price cost, assumptions, supplied metadata labels, uncertainty/confidence, routing, and rationale.
- Aggregate estimation metadata is exact-key/bounded/secret-rejecting before serialization; invalid context never reaches the provider. Model-authored actual-route claims are discarded, host-observed route metadata wins, and invalid token telemetry fails closed.
- Optimizer-call telemetry is measured but does not change the estimate-only status; task-execution telemetry can reconcile the same estimation ID later.
- UI corrections are implemented: hover/focus context info, taller editor, controls outside editor, Criticality choices plus reserved `auto`, compact/scrollable Review, accessible title/subtext ellipses, and projection disclosure without prompt mutation.
- Prompt Optimizer is now the sole native Activity Bar product/container/view. The internal webview rail retains Home, Project Meta Context, Library, Activity, Docs, and Settings capabilities without separate native identities.
- `progress.html` retains the unchanged canonical iframe and plugin preview and now presents the reconciled Phase 0–7 roadmap.
- The 27-path functional candidate was committed with no program records; the local remote-tracking ref currently also resolves to that SHA, but no live remote verification was attempted in this run.

## Verification status

- Focused tests: domain semantic/projection 69; host transport/activity 24; webview UI 6.
- Fresh current suite evidence: 420 passed (104 + 31 + 66 + 16 + 172 + 31); the earlier catalog loopback environment failure is resolved.
- All six package typechecks: passed.
- Schema export: unchanged.
- Build and VSIX: passed.
- Phase 4 exact review SHA: `ff0bbea30b2e502bc3c5b9287da003994fc13ce0`; current local implementation/evidence tip: `aa2a1ba`.
- Fresh final VSIX package: 388 files, 684,331 bytes; SHA-256 `114fc30d850036d1ceb7cadea0845aa43b0e3f5e0dd1e416609cd071568ce245`.
- Fresh VS Code 1.134.0 installed smoke passed installation, activation, Home open, distinct Prompt Optimizer open, extension-host exit 0, and the terminal marker.
- Fresh clean absence/install/uninstall/absence/fresh-install: `DWI_CLEAN_REINSTALL_OK`.
- Fresh `pnpm verify:extension:restart` passed against that VSIX in a disposable portable profile; the direct macOS run used native consent sheets and three separate VS Code processes with graceful termination between them, then removed the temporary profile in `finally`.
- Model-host verification under Node 24.18.0 passed 16 syntax checks, 20 active tests, and recovered simulator checks.
- Frozen contract/corpus SHA-256: `c8dbded44e96933bf28c92d534831136445e5b0814ded115fcb8dcff27a8272b` / `ca88bfbad8b018e6898024df61ca3b0d221c55b8094722fb6597094763df4aa7`.
- Single live attempt: one readiness request returned HTTP 200 in 5,634 ms and reported 14,531 tokens (14,526 input; 5 output), exceeding the frozen 12,000 per-completion ceiling. Semantic calibration and fixed evaluation did not start; no corpus content was transmitted and no retry occurred.
- Canonical HTML: unchanged.
- `progress.html`: Firefox 154.0.1 renders at 1440×1000, 1440×1800, and 1440×3600; seven local targets and inline JavaScript syntax pass; SHA-256 `ac8f2bb9e224af64d1f009c3f5985391ed1360483330fdceec3816c07b0020f0`.
- Current Phase 4 checklist: 14 / 18 checked; 4 pending.
- Provider calls in this evidence session: one bounded readiness request only. The exact-SHA external CI had already passed; this session performed no remote mutation, external CI, merge, tag, or release action.
- Lint: unavailable because `eslint` is not installed; no pass claimed.
- Language-fixture verification: unavailable because `/Users/vk/Projects/dwi-language-fixtures` does not exist; no pass claimed.

## Pending / blockers

- The installed harness now proves initialization/review → optimizer save → optimizer-only reset → approved-project preservation both in one process and across three processes against the same portable profile.
- Local commit authority was supplied by the 2026-09-01 POC request; commits through `c8d1723` and the evidence correction are local only.

- Production semantic-plus-projection calibration remains pending: the readiness response exceeded the immutable per-call token ceiling before the semantic request.
- The 30-run fixed A/B/C comparison remains pending because the calibration stop condition fired first.
- Any second attempt requires explicit authorization to supersede the no-retry contract and preserve the failed lock/evidence. Do not delete the lock, change the existing evidence, or silently raise thresholds.
- Phase 4 semantic-plus-projection calibration and fixed-executor comparison remain blocked and cannot be silently retried or waived.
- Any provider/fixed-executor transmission, cost, credentials, push, merge, tag, release, or promotion remains explicitly unauthorized. Broad local Phase 6 evidence collection was authorized.
- The single authorized desktop Rewrite click on 2026-09-01 was stopped by the current-project review gate before provider dispatch; it was not retried. The restart verifier separately exercises only the provider-free deterministic path and does not claim a provider call.

## Known deviations

- The explicit same-branch instruction overrides the default one-branch-per-phase topology.
- Repository lint remains unavailable because `eslint` is not installed.
- The optional language-fixture checkout expected at `/Users/vk/Projects/dwi-language-fixtures` is absent.

## Next agent entry point

Preserve exact Phase 4 SHA `ff0bbea30b2e502bc3c5b9287da003994fc13ce0`, the failed live lock, checkpoint, and raw evidence. Phase 5 and Phase 6 local POC evidence now includes the packaged installed restart/resume/reset pass; do not confuse it with Phase 4 semantic/fixed-executor qualification. Do not retry Phase 4, transmit provider/fixed-executor input, use credentials, or perform remote/promotion actions without explicit authorization.

Phase status: Phase 0 DONE; Phases 1–3 DONE locally; Phase 4 BLOCKED — immutable no-retry calibration budget exceeded and fixed evaluation not started; Phase 5 DONE locally — POC implementation and packaged restart/resume/reset evidence pass; Phase 6 DONE locally — broad POC qualification matrix pass; Phase 7 not started.
