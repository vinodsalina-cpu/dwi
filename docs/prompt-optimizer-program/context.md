# Current recovery context

Updated: 2026-09-03 UTC

## Repository identity

- Repository: `vinodsalina-cpu/dwi`
- Recovery branch: `recovery/exact-tree-requalification-2026-09-03`
- Verified base: `origin/main@f1c69f8d713a32af0ef011946ec0a3bad28d8120`
- Latest product-code commit: `5088dbf6e5fdeea2415b991c429de8e5229d7a7b`
- `origin/feature/po-05-product-hardening`: `f1c69f8d713a32af0ef011946ec0a3bad28d8120`
- Preserved walkthrough ref: `origin/main-temporary-demo-archive@bb2ea52935465ec73859fb7a47af936af442d263`
- Pre-vNext tag: annotated `dwi-pre-optimizer-vnext-baseline` at `2fa86e1079e3aec9c1957144164825fd43e6141d`
- Final standalone-baseline tag: absent
- Branch base was clean with zero divergence from `origin/main`. Current recovery-document edits are intentional and uncommitted until the Phase 0 record is reviewed.

## Product truth

DWI is a consent-based project-initialization and reviewed Project Meta Context experience. Prompt Optimizer depends on that approved knowledge. Input → Resolve → Review navigation and deterministic compilation are provider-free. The only provider path in this recovery is an explicit user action routed to an out-of-process loopback OpenAI-compatible test server. Real provider and fixed-executor lanes remain unauthorized and blocked.

The host owns workspace/session identity, revisions, base hashes, networking, provider settings, and secrets. Accepted semantic changes must be valid, current, bounded, hash-bound V2 patches. The local candidate survives and is visibly labeled on every semantic failure. Question and semantic-call ceilings remain three; no fourth call may exist.

## State ownership correction

- `.dwi/project.yaml`: the only intended checked-in DWI state.
- Initialized project state: beneath `context.globalStorageUri/workspace-state/<localFingerprint>/.dwi/.managed/`.
- Consent receipts and provider settings: extension `globalState`.
- Provider credentials: `SecretStorage`.
- Optimizer recovery sessions and current view: extension `workspaceState`, keyed by checkout-local fingerprint.
- Optimizer-only reset must retain approved project knowledge, declaration, consent, and provider settings.

## Inherited-statement classification

| Statement | Classification | Current treatment |
|---|---|---|
| `main`, `origin/main`, and the product-hardening branch resolve to `f1c69f8`; product code ends at `5088dbf` | current | Verified from fetched refs and Git history on 2026-09-03. |
| Root tests previously passed 421 and exact-SHA workflow `33576506566` passed | exact-candidate historical | Useful baseline only; rerun on the recovery candidate. |
| Phase 5/6 local POC gates passed on earlier packages | historical | Preserve in phase outcomes; do not transfer to the new package hash. |
| Current exact tree has fresh three-process persistence proof | contradicted | No transferable current-tree raw evidence exists; rerun is required. |
| `expected.md` is the active Phase 5 contract | contradicted | Archived under `phases/`; this recovery contract is active. |
| Initialized managed state is stored in the project checkout | contradicted | Source constructs it beneath extension global storage. |
| DWI-PO-0033 was ledgered | contradicted | Referenced in old contract and by DWI-PO-0034, but absent from the ledger; corrected by appended DWI-PO-0036 without inserting history. |
| The lost five-hour Sol/max run passed | unverified historical testimony | Preserve the user's statement only; no result is reconstructed. |
| Phase 4 readiness exceeded 12,000 tokens and stopped before calibration/evaluation | historical tombstone | Preserve exactly: 14,531 tokens, calibration 0/1, optimizer 0/8, fixed executor 0/30, no retry. |
| Prior VSIX files, evaluation corpus/results, local archives, external fixtures, and extension storage survived | contradicted | Git contains summaries only; regenerate new evidence under new hashes. |

## Current recovery boundary

Phase 0 freezes this contract and inventory. The immediate execution order is: pinned true clean clone; deterministic/package gates; installed one-process journey; portable three-process continuity; loopback semantic success/failure/copy; minimum VS Code compatibility; independent review; exact-SHA CI. Native macOS consent remains a separately classified platform lane.

The next product slice—one focused clarification question at a time—remains parked until recovery closes.
